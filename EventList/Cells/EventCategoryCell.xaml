<?xml version="1.0" encoding="UTF-8"?>
<ContentView 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="EventList.EventCategoryCell" 
    xmlns:local="clr-namespace:EventList;assembly=EventList">
    <StackLayout Spacing="{DynamicResource CardSpacing}">
        <local:CardView HeightRequest="100">
            <StackLayout Orientation="Horizontal" HorizontalOptions="StartAndExpand" VerticalOptions="CenterAndExpand" Padding="10,0,0,0">
                <Label Text="{Binding Name}" Style="{DynamicResource EvolveListItemTextStyle}" TextColor="#000000"/>
            </StackLayout>
        </local:CardView>
    </StackLayout>
</ContentView>